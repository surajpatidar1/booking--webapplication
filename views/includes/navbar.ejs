<style>
  .btn-search{
    background-color: #2a2424;
    color: #fff;
    border-radius: 20px;
    
  }
  .btn-search:hover{
    background-color: #2a2424;
    color: #fff;
  }
  .btn-search i{
    background-color: #2a2424;
    color: #fff;
    display: inline;
    margin-right: 0.5rem;
  }
  .serach-inp{
    border-radius: 25px;
    padding: 0.5rem 3rem 0.5rem 3rem;
  }
  #search{
    margin-right: 25rem;
  }
  #btw{
    margin-top: 6px;
  }
</style>


<nav class="navbar navbar-expand-md navbar-light bg-light boder-bottom sticky-top">

    <a class="navbar-brand" href="/listings"><i class="fa-solid fa-bezier-curve"></i></a>
    <button
              class="navbar-toggler" 
               type="button" 
               data-toggle="collapse"
                data-target="#navbarNav" 
             aria-controls="navbarNav" 
                aria-expanded="false"
              aria-label="Toggle navigation">
             <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav" class="navbar-nav">
          <a class="nav-link" href="/listings">Explore</a>  
    </div>

  
    <div class="navbar-nav ms-auto" id="search">
      <form class="form-inline" action="/search" method="post">
        <input class="form-control mr-2" type="search" name="search" placeholder="Search Destinations" aria-label="Search" id="searchInput" />
        <button class="btn btn-search" type="submit">
          <i class="fa-solid fa-magnifying-glass"></i>Search
        </button>
      </form>
    </div>

    <div class="navbar-nav ms-auto" >
      <a class="nav-link collapse navbar-collapse" href="/listings/new">Add new Listing</a> 
      <p id="btw">|</p>
      <% if(!currentuser){%>
        <a class="nav-link collapse navbar-collapse" href="/signup"><b>Signup</b> <span class="sr-only">(current)</span></a>
        <p id="btw">|</p>
        <a class="nav-link" href="/login"><b>Login</b> <span class="sr-only">(current)</span></a>
        <%} %>
        <% if(currentuser){%>
      <a class="nav-link collapse navbar-collapse" href="/logout"><b>Logout</b> <span class="sr-only">(current)</span></a>
      <%} %>

    </div>
  </nav>
  
  

  